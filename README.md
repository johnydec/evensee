# evensee
Chrome extension that captures auth headers (cookies, tokens) from browser   requests with one click. Ready-to-paste JSON for Playwright &amp; AI coding agents. Zero network,   memory-only, auto-clear.
